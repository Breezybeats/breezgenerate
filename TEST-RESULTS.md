# Validation report — 2026-09-07

## Actually run and passed in the creation environment

Environment: Linux x86-64, GCC 13.3, C++17. Core CMake configure/build succeeded using CMake 4.4.3. These are not Mac binary results.

- Portable DSP CTest suite passed.
- Same seed/settings produce identical parameter values and table arrays.
- Every parameter group lock and WT lock tested.
- FFT inverse roundtrip and STFT identity/512-sample alignment verified.
- Every named non-Auto family rendered at 44.1, 48, 88.2 and 96 kHz with finite, non-silent, sample-peak-bounded output.
- 200 additional automatically selected seeded patches rendered at varied MIDI pitches, without silence or nonfinite/unsafe output.
- Eight simultaneous voices with extreme parameters remained finite and within the default safety ceiling.
- Note-off, sustain pedal, mono last-held-note return and deterministic block-partition playback checked.
- Invalid floating-point parameter input was repaired.
- Bounded RMS calibration checked.
- GCC AddressSanitizer + UndefinedBehaviorSanitizer run passed; leak detection disabled for that run. No claim about long-duration leak testing.
- JUCE processor/editor, state test and host-smoke source passed `g++ -fsyntax-only` against actual JUCE 8.0.6 headers. This is a syntax/type check, not linking.
- The audio preview was generated by the compiled portable DSP engine, not by a sample player or an image/UI mockup.

Test-window uncalibrated RMS across the 29-family/four-rate suite was approximately 0.040–0.094. This is an engineering measurement over a short note, not a judgement of loudness matching, mix readiness or musical quality.

## Configured in GitHub CI, NOT executed here

- Mac arm64 VST3 + standalone compile/link.
- Processor preset state and rendered audio recall tests, 90 factory recalls, undo/redo and invalid state rejection.
- Actual VST3 scan/load as an instrument, MIDI, advertised latency, finite audio at 28 sample-rate/block-size combinations.
- Editor creation/resizing inside a JUCE host.
- Ad-hoc bundle signing and signature/architecture verification.

The local machine lacks Linux GUI/audio development libraries. Installing those system libraries was blocked by permissions, so full local linking was not attempted further. The first successful Mac GitHub workflow run is required before claiming a compiled Mac plugin exists.

## Not performed

- FL Studio scan/playback/automation/save-reopen/offline export on your M1.
- Mac security approval/notarisation testing.
- Manual listening-based preset curation or comparison to LOBOTIX/J-core reference recordings.
- Extended CPU, crash soak, thread-sanitizer, true-peak or professional audio quality testing.
- Windows, Intel Mac or universal binaries.

If the workflow fails, provide the first compiler/test error from its failed step; the source can then be corrected against the actual Mac result.
